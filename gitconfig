[push]
  default = current
[user]
  name = Jing LIU
  email = jing.liu@usd.edu
[alias]
  br = branch
  co = checkout
  ci = commit
  st = status
[filter "lfs"]
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
  clean = git-lfs clean -- %f
